version: '3.1'
services:
  server:
    build:
      context: ../distributed_social_network
      dockerfile: Dockerfile
    restart: always
    environment:
      DB_NAME: social_network
      DB_USER: username
      DB_HOST: db
      DB_PASSWORD: password
      DB_PORT: 5432